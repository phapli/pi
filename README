asb
---

Ansible for lazy sysadmin
-------------------------

Install

  ~$ # Add ppa
  ~$ sudo apt-add-repository ppa:ansible/ansible

  ~$ sudo apt-get update
  ~$ sudo apt-get install ansible

Run

  ~$ # main role
  ~$ ansible-playbook -i hosts main.yml -vvvv